export const environment = {
  production: true,
  // IMPORTANT (GitHub Pages): there is no backend, so "/api/..." will 405.
  // Put here the full origin of your hosted openai-proxy, e.g. "https://mentormatch-api.yourdomain.com"
  openAiProxyBaseUrl: '',
};


